# [任务名] Plan

`plan.md` 是单文件规格驱动执行合同。

一个文件管总。

把下面内容替换成当前任务的真实内容。不要保留占位符。

## 1. 需求文档

写给用户看的非技术说明。

必须回答：

- 用户真正要解决什么实际问题。
- 谁会使用这个能力。
- 用户完成任务时应该看到什么体验。
- 当前范围包含什么。
- 当前范围不包含什么。
- 怎样算业务上完成。

要求：

- 写 WHAT 和 WHY。
- 不写框架、文件、函数、数据库、接口等实现细节。
- 不把用户建议的做法直接写成业务需求。

## 2. 当前事实

写已经确认的仓库事实。

- 当前代码事实：
- 当前测试事实：
- 当前文档事实：
- 当前配置事实：
- 当前命令输出事实：
- 当前已存在能力：
- 当前缺口：
- 当前未知点：

要求：

- 只写证据支持的事实。
- 旧提交、外部项目、用户偏好只能作为证据来源。
- 未确认内容写成线索或未知点。

## 3. 失败测试

先写什么会失败。

- 自动测试：
- 类型检查：
- 构建命令：
- CLI 输出检查：
- 日志或状态检查：
- 真实用户路径演练：

要求：

- 每条失败测试必须对应真实产品行为或真实工程风险。
- 能自动测就自动测。
- 不能自动测就写清楚手动检查命令和期望输出。

## 4. 目标

写最终要交付的可验收结果。

- 用户路径完成到：
- 代码主链路接到：
- 状态和记录落到：
- 输出呈现到：
- 测试和文档同步到：

要求：

- 可观察。
- 可验证。
- 可判定完成或失败。

## 5. 不做范围

写清楚本次不做什么。

- 不做：
- 不保留：
- 不兼容：
- 不处理：

要求：

- 不做范围必须服务边界清晰。
- 不写假兼容。
- 不写 speculative 内容。
- 不保留当前产品没有的能力入口。

## 6. 设计

写技术设计。

### 主链路

输入 -> 判断 -> 状态 -> 执行 -> 输出 -> 记录

### 模块边界

- 模块：
- 负责：
- 不负责：

### 文件职责

- 文件或目录：
- 职责：
- 变化原因：

### 状态归属

- 状态：
- 归属：
- 读写入口：

### 数据或事件流

- 来源：
- 处理：
- 落点：
- 呈现：

### 错误、恢复、中断、重试

- 错误：
- 恢复：
- 中断：
- 重试：

### 测试和文档影响

- 测试：
- 文档：

## 7. 实施任务

写可执行 checklist。

每项必须是动作，并尽量包含文件、依赖和验收方式。

- [ ] T001 [动作] in [文件/模块]；依赖：[依赖]；验收：[检查方式]
- [ ] T002 [动作] in [文件/模块]；依赖：[依赖]；验收：[检查方式]
- [ ] T003 [动作] in [文件/模块]；依赖：[依赖]；验收：[检查方式]

## 8. 验证计划

写最后怎么证明完成。

- [ ] 局部测试：
- [ ] 完整验证命令：
- [ ] 构建或安装检查：
- [ ] CLI/UI/远程入口检查：
- [ ] 恢复或中断演练：
- [ ] 文档同步检查：
- [ ] 未验证内容：
- [ ] 剩余风险：

## 9. 收口

任务结束时更新。

- 目标是否完成：
- 失败测试是否变绿：
- 改了哪些文件：
- 跑了哪些验证：
- 哪些内容没有验证：
- 剩余风险：
- 是否 commit / push：

## 简短样例

### 1. 需求文档

用户启动应用时，需要清楚知道当前可以继续哪个会话，也可以直接新建会话。完成标准是用户不用记 session id，也能恢复最近工作。

### 2. 当前事实

- 当前代码已有 session 存储。
- 当前 CLI 默认直接进入新会话。
- 当前测试没有覆盖启动时会话选择。

### 3. 失败测试

- `node dist/cli.js` 不显示最近会话列表：失败。
- 选择 `0` 后没有新建会话：失败。
- 选择已有编号后没有恢复对应会话：失败。

### 4. 目标

启动 CLI 时展示最近会话，`0` 新建，其他编号恢复对应会话。

### 5. 不做范围

不做多设备同步。不做云端会话列表。不兼容旧的隐藏选择入口。

### 6. 设计

CLI 入口读取 session store，渲染编号列表，用户选择后把 session id 交给现有 interactive 主链路。

### 7. 实施任务

- [ ] T001 添加 session picker 输出；验收：CLI 启动显示编号。
- [ ] T002 接入 interactive 入口；验收：选择编号恢复会话。
- [ ] T003 补测试；验收：新增测试通过。

### 8. 验证计划

- [ ] `npm.cmd run typecheck`
- [ ] `npm.cmd run verify`
- [ ] 手动运行 `node dist/cli.js`

### 9. 收口

完成后写实际结果、验证命令和剩余风险。
